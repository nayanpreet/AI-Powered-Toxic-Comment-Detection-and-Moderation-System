# Run Guide

## 1) Setup
```bash
pip install -r requirements.txt

Open and run:
	•	ML_Final_Project_code_notebook.ipynb

Run the cells in order for training/evaluation/inference.

Trained weights + tokenizer are located in:
	•	toxicity_model/

model.safetensors is tracked using Git LFS.